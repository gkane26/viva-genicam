# Backlog

Immediate, actionable tasks. Mid-term direction lives in
[roadmap.md](roadmap.md); shipped work is recorded in
[CHANGELOG.md](../CHANGELOG.md).

**Legend**

- Priority: **P0** (do next) / **P1** (soon) / **P2** (when convenient)
- Size: **S** (hours) / **M** (a day or two) / **L** (several days) /
  **XL** (a week+)
- Status: `planned` / `in-progress` / `blocked`

Every row carries its evidence — a corpus count, a `file:line`, or an issue
number — so priority can be argued from data rather than from intuition
([ADR-0018](adrs/adr0018-genapi-conformance-over-convenience.md)).

**This file only lists open work.** A row is deleted when it ships; the record
of what shipped, and why, is [CHANGELOG.md](../CHANGELOG.md). It used to keep
its completed rows, and at 69 of them the working list was 131 KB of mostly
history.

Two rules that came out of rows deleted this way, kept because they are about
how to file a row rather than about any one defect:

- **Do not propose downgrading a log level before the warning's cause is
  known** — that is a proposal to delete the evidence. A row once read a
  per-frame `WARN chunk header truncated remaining=6` as cosmetic and guessed
  the fake was appending bytes it should not. The fake was emitting a
  spec-conforming trailer and the parser was reading it at the wrong offset;
  the warning was the only outward sign of a defect that silently broke chunk
  decoding on every real camera.
- **A row filed from fake-camera output alone is a hypothesis.** That one only
  became obvious when a real JAI emitted the identical `remaining=6`, which is
  what showed the fake was not the variable.

## REL — Releases

| ID | Task | Priority | Size | Status | Notes |
|----|------|----------|------|--------|-------|
| REL-08 | Cut 0.6.0 | P0 | S | planned | **Minor, not a patch**, for three independent reasons. `NodeDecl::Integer` **has gained** `byte_order` ([ADR-0022](adrs/adr0022-integer-register-decoding.md)) and `#[non_exhaustive]` sits on the `NodeDecl` *enum* rather than the variant (`crates/viva-genapi-xml/src/lib.rs:607`), so struct-variant literals broke — one in the studio workspace, the other two sites there being patterns with `..` rather than constructions. `DeviceAnnounce` gains fields and has no `#[non_exhaustive]` (`crates/viva-zenoh-api/src/lib.rs:31-42`). And `^0.5` resolves to any 0.5.x, so a patch that changes what 448 registers decode to would reach dependents on their next `cargo update` — the same trap that forced 0.4.0 to be a minor. The integer codec half has shipped; still gated on TC-22/TC-23/GA-31 and SVC-07/ST-24. Six version touchpoints **plus** the intra-workspace caret ranges, which a minor makes unsatisfiable |

## TC — Transport conformance (roadmap Phase 1, ADR-0019)

The GVCP/GVSP audit ADR-0018 never reached. Opcodes cross-checked against
`../aravis/src/arvgvcpprivate.h:267-282`.

| ID | Task | Priority | Size | Status | Notes |
|----|------|----------|------|--------|-------|
| TC-05 | Accept the payload types cameras actually send | P1 | M | planned | **This entry's own premise was wrong and is corrected here.** It claimed Image Extended Chunk (0x4001) "never opens a frame". It does open one: `parse_leader` compares `cursor.get_u16() as u8` against `PAYLOAD_TYPE_IMAGE` (`crates/viva-gige/src/gvsp.rs:266`), and `0x4001 as u8 == 0x01`, so the truncating cast lets it through — and then stores `payload_type: 0x01`, discarding the fact that the block carries chunks. So the defect is not rejection but *silent misclassification*, which is why #70's JAI streamed at all. What is genuinely rejected is any payload type whose **low byte** is not 0x01 (0x0002 raw data, 0x0004 file), and packet format 0x04 (single-packet block, `gvsp.rs:235-240`). Separately `parse_packet:233` derives a header-level payload type from `payload[0..2] >> 4` — those two bytes are the GVSP **status** word, not a payload type — and passes it to `parse_leader` as `_payload_type_header`, which discards it. The trailer-offset half of this was fixed in 0.4.0. **Second hardware sighting 2026-08-03** ([#93 comment](https://github.com/VitalyVorobyev/viva-genicam/pull/93#issuecomment-5162882332)): a Micro-Epsilon scanCONTROL 850050 on firmware 3.0.0 sends leader payload type `0x4001` with PFNC `0x026000C0`, read from a raw GVSP capture by the contributor. Two vendors and two device classes now send the type we silently downgrade to `0x01`, so the misclassification is the common case for chunk-capable devices rather than a JAI quirk |
| TC-06 | Chunk trailer layout is self-consistent, not conformant | P1 | M | planned | `parse_chunks` decodes front-to-back big-endian (`gvsp.rs:112-143`) while `crates/viva-genicam/src/chunks.rs:114-142` reads values little-endian. Real cameras append backward-scanned `[data][id][len]` tuples. The fake emits our layout, so the round trip proves nothing |
| TC-10 | `viva-gencp::encode_cmd` is dead *and* divergent | P2 | S | planned | It writes a different header shape than `viva-gige` actually uses and has no callers outside its own tests (`crates/viva-gencp/src/lib.rs:179-188`) |
| TC-11 | `mtu()` returns a hardcoded 1500 off Linux | P2 | M | in-progress | `crates/viva-gige/src/nic.rs`. Jumbo frames can never be selected on macOS. The Windows half is **resolved** by [#72](https://github.com/VitalyVorobyev/viva-genicam/pull/72) (`GetIfEntry2`); macOS remains |
| TC-12 | Settle the `PENDING_ACK` field width against hardware | P2 | S | planned | GigE Vision 1.2 §18.5 defines 2 reserved bytes + a `u16` `time_to_completion`; aravis reads all four payload bytes as a big-endian `u32`, as does our own `viva-u3v` (`control.rs:190-199`). The readings agree only while `reserved` is zero. Our GVCP `PENDING_ACK` handler implements the spec `u16` and clamps it. A capture from a camera that actually sends one decides it — and if a real device puts the timeout in the full `u32`, that device wins. **The U3V half is settled**: aravis's `ArvUvcpPendingAckInfos` is `{ guint16 unknown; guint16 timeout; }` read little-endian, matching the spec layout, so `viva-u3v` now reads a LE `u16` at offset 2 (TC-16). The earlier claim here that aravis reads a big-endian `u32` holds only for the *GVCP* path, not the U3V one. GVCP remains open |
| TC-16 | One flat `StatusCode` cannot serve both GVCP and GenCP | P0 | M | in-progress | The tables share `0x0000`, `0x8001`-`0x8007` and `0x8FFF`, then **conflict at the same code**: `0x800B` is `NO_MSG` (deprecated) in GVCP but `MSG_TIMEOUT` in GenCP. GVCP additionally owns the packet-resend family (`0x0100`, `0x800C`-`0x8017`); GenCP owns `0xA001`-`0xA101`. So adding entries cannot make the shared enum correct — it needs a shared core plus per-transport extension. **Write an ADR.** This also corrects the `PENDING_ACK` rationale we recorded for U3V, and that half is worse than "unrecognised" — it is **silent wrong data**. `decode_ack` reads the acknowledgement's command id into `let _opcode` and discards it (`viva-u3v/src/control.rs:267`), and `transact` validates only `request_id` (`:180`). A GenCP pending-ack is a distinct command (`0x0805`) carrying `time_to_completion` in its SCD, with a *success* status — so on a device that uses one we accept it as the real answer and **return the timeout bytes as the register value**. A 4-byte `ReadRegister` gets a plausible-looking wrong number. Meanwhile `StatusCode::Unknown(0x8006)` (`:29`, `:189`) only ever fires on a genuine `ACCESS_DENIED`, which we then sleep-and-retry 100× (`MAX_PENDING_RETRIES`) before reporting `Timeout` instead of "access denied". Both halves wrong, mutually reinforcing. aravis gets this right and shows the shape of the fix (`arvuvdevice.c:344-373`): read status *and* command id, test `command == PENDING_ACK` first and independent of status, then require `command == expected_ack_command` before accepting the payload — we already have `OpCode::ack_code()` for that comparison. The test at `control.rs:472` asserts the misreading back to us: fourth occurrence of the pattern where the fake asserts our own misreading back to us ([ADR-0019](adrs/adr0019-transport-conformance-and-spec-derived-fakes.md)). The `StatusCode::from_raw` table was corrected in 0.4.0, which already made the `Unknown(0x8006)` arm dead code. **Done so far:** pending-ack is keyed off command id `0x0805` (`viva_gencp::PENDING_ACK_COMMAND`), `decode_ack` keeps the command id, `transact` requires it to equal `OpCode::ack_code()` before accepting a payload, and the SCD timeout is read as a little-endian `u16` at offset 2 — the old big-endian `u32` read turned a 1 000 ms request into 59 395 ms. `build_ack` now takes an explicit command id, deleting the "opcode doesn't matter for most tests" comment that was the assumption behind the whole defect. **Remaining:** the per-transport wrapper types (ADR-0020 points 1-2). Until they land a transport-specific code decodes as `Unknown(raw)` — reportable but unnamed; `0x800B` is the forcing case and no current code path decodes it, so this is safe to defer to the next breaking window beside API-02/API-03 |
| TC-18 | Stale-ack absorption is bounded by a count, not a deadline | P2 | S | planned | Noted while merging [#91](https://github.com/VitalyVorobyev/viva-genicam/pull/91), and promised to its author. That PR correctly stopped a mismatched acknowledgement from triggering a resend — a retransmission with a fresh request id is indistinguishable from a new command, which is how a non-idempotent `WRITEMEM` gets executed twice — and now absorbs unrelated datagrams while waiting. But it bounds the absorption by a *count* (`mismatched_acks < MAX_RETRIES`) and each `recv_absorbing_pending` call restarts a full `CONTROL_TIMEOUT`, so a device trickling stale acks can stretch one attempt to `4 × 500 ms` rather than capping at a single 500 ms window. aravis bounds the same window by wall clock instead (`arvgvdevice.c`, `timeout_stop_ms` recomputed only on PENDING_ACK). Bounded either way and strictly better than resending, so this is a latency shape to revisit if it appears on hardware, not a defect. **Negative data point 2026-08-03** ([#93 comment](https://github.com/VitalyVorobyev/viva-genicam/pull/93#issuecomment-5162882332)): #91's own author stress-tested a scanCONTROL 850050 on firmware 3.0.0 and saw no slow control transactions, stale acknowledgements or retries — ROI and line-rate changes were accepted during a live stream without timeouts. One device is not a clearance, but nothing has pushed this above P2 |
| TC-21 | `BAD_ALIGNMENT` on a real camera's event registers | **P1** | M | planned | **Seen on hardware** ([#112](https://github.com/VitalyVorobyev/viva-genicam/issues/112), attached Studio log): **62** reads of `pEventStream*Reg` on a Vieworks FS3200T return `device reported status BAD_ALIGNMENT (0x8005)`. We have been here — the Hikrobot in [#35](https://github.com/VitalyVorobyev/viva-genicam/issues/35) rejected unaligned READMEM counts, and `read_mem` now rounds the *count* up to a multiple of four (`crates/viva-gige/src/gvcp.rs:1103-1120`). This is the other half: the *address*. Nothing rounds or checks it, and a `<Register>`/`<IntReg>` whose `<Address>` is not 4-byte aligned goes to the wire as-is. Confirm against the camera's XML before assuming — ask the reporter for it, per the corpus rule, since that also settles whether these are `<IntReg>` at an odd address or something else entirely. Note the status code only reads correctly at all because the `StatusCode::from_raw` table was corrected in 0.4.0; before 0.4.0 this would have printed `device busy`. **TC-22 probably does not fix this**, and should not claim to: dispatching aligned 4-byte access to READREG leaves an unaligned address unaligned, and covering an 8-byte event register would need multi-register batching, which TC-22 deliberately excludes. Still needs the reporter's XML |
| TC-22 | Every register access is READMEM/WRITEMEM, never READREG/WRITEREG | P1 | M | planned | [#136](https://github.com/VitalyVorobyev/viva-genicam/issues/136), from a Wireshark capture. `GigeRegisterIo::read`/`write` (`crates/viva-genicam/src/lib.rs:802-823`) call `device.read_mem`/`write_mem` unconditionally — no width or alignment dispatch — so a 4-byte `IntReg`, a bitfield read-modify-write and a `<Command>` write all go out as memory access. The bootstrap helpers do it too, and **already inconsistently**: `set_stream_packet_size` writes `GevSCPSPacketSize` with WRITEMEM (`crates/viva-gige/src/gvcp.rs:1252`) while `get_stream_packet_size` reads the same register back with READREG (`:1294`). `read_register`/`write_register` already exist (`:1087`, `:1107`). Dispatch when `len == 4 && addr % 4 == 0 && addr <= u32::MAX`; READREG_ACK's 4-byte field re-serialised with `to_be_bytes()` reproduces exactly what READMEM returned, so the switch is byte-transparent to `RegisterIo` callers. Needs a per-device fallback latch and a `VIVA_GIGE_FORCE_READMEM` escape hatch, because the project's whole thesis is that devices are non-conformant. **Do not batch**: a multi-register READREG has no defined atomicity, so a latched timestamp can tear silently — worse than the error it replaces — and WRITEREG's ack carries only a data-index placeholder, so a partial multi-register write is unreportable. **That is also why this is probably *not* a fix for TC-21**, which would need batching. Also fix the `read_register`/`write_register` misnomer in `crates/viva-gige/src/time.rs:363-370`, which forwards to `read_mem`/`write_mem` |
| TC-23 | `viva-fake-gige` cannot disagree with us about register access | P1 | M | planned | Prerequisite for TC-22 and GA-31 being testable at all, and an instance of the realism policy ([ADR-0019](adrs/adr0019-transport-conformance-and-spec-derived-fakes.md)). Three gaps. (1) No per-opcode counters, so no test can assert *which* command left the host. (2) `take_pending_test_packet`/`fire_test_packet` are called only from `handle_writemem` (`crates/viva-fake-gige/src/gvcp_server.rs:361`, `:377-379`); `handle_writereg` has no equivalent, so switching `request_test_packet` to WRITEREG would silently kill the GVSP path probe and the whole packet-size suite (`crates/viva-genicam/tests/fake_camera.rs:583,633,727,819,850,885`). (3) `RegisterMap::read` enforces no access mode (`crates/viva-fake-gige/src/registers.rs:1096-1130`) — a READMEM of a WO address returns stored bytes happily — and the four WO nodes it declares (`:559`, `:579`, `:586`, `:719`) have no bitfield, so GA-31's path has zero in-tree coverage. Also wanted: an option to refuse READREG (`NOT_IMPLEMENTED`, and separately no reply at all), which is the only way to test TC-22's fallback without hardware |

## DX — Diagnostics loop (roadmap Phase 2)

Every fix so far was diagnosed from a user-supplied artifact, and the library
offers no supported way to produce one.

| ID | Task | Priority | Size | Status | Notes |
|----|------|----------|------|--------|-------|
| DX-05 | Surface skipped nodes in Python and Studio | P1 | S | planned | `NodeMap::skipped()` already carries them out through `viva-camctl` (0.4.0). `viva-pygenicam` and Studio still cannot ask: a Studio user seeing a missing feature has no way to learn it was dropped rather than absent |
| DX-06 | `viva-camctl report` for USB3 Vision | P2 | M | planned | The bundle is GigE-only — it opens a GVCP control channel and dumps GigE bootstrap registers. A U3V reporter gets nothing equivalent, though `viva-camctl list-usb` exists |
| DX-11 | `viva-camctl set` reads back unconditionally, so a write-only write reports failure | P1 | S | planned | `crates/viva-camctl/src/cmd_set.rs:35-37` reads the node after every successful `set`. On a `WO` node that read is refused, so the command reports failure after the write succeeded — and on a real camera it puts a rejected request on the wire. Gate the read-back on the node being readable. Found while tracing [#135](https://github.com/VitalyVorobyev/viva-genicam/issues/135); it is the likeliest thing that report actually ran |

## SR — Streaming reliability (roadmap Phase 3)

| ID | Task | Priority | Size | Status | Notes |
|----|------|----------|------|--------|-------|
| SR-03 | Per-stream ephemeral ports + `source_filter` enforcement | P0 | M | planned | `source_filter` is configured at `crates/viva-genicam/src/stream.rs:270-274` and never read, because `PacketSource::recv` discards the source address (`stream.rs:51-61`) |
| SR-04 | Wire packet resend end-to-end, or delete it | P1 | L | planned | `ResendPlanner` (`gvsp.rs:490`), `request_resend` (`gvcp.rs:1004`) and `resend_enabled` have zero production callers. `README.md:26` advertises resend as shipping — pick one. **Worse than dead code**: `StreamStatsAccumulator::record_resend` (`stats.rs:119`) also has no production caller — its only caller is `examples/stats.rs`, which fabricates the number — so `resends=0` in every real `viva-camctl stream` summary reads as "the network is healthy" when it means "not implemented". Same for `resend_ranges` and `backpressure_drops`. Until this lands, `drops` is the only reliability figure worth reading; `book/src/tutorials/streaming.md` §4.3 now says so |
| SR-07 | Honest streaming telemetry | P1 | M | planned | `resends`, `resend_ranges`, `late_frames`, `pool_exhaustions`, `backpressure_drops` are permanently zero — nothing in the live path calls their recorders. Every GVSP parse error is swallowed at `trace` and counted nowhere (`stream.rs:630-636`) |
| SR-08 | IGMP leave on multicast stream teardown | P2 | S | planned | `join_multicast_v4` is called (`nic.rs:322`); nothing ever leaves the group |
| SR-09 | Delete or wire the unused reassembly stack | P2 | M | planned | `Reassembler`, `FrameAssembly`, `FrameQueue`, `BufferPool` and `GigeDevice::negotiate_stream` have no callers; `StreamBuilder::build` re-implements `negotiate_stream` inline (`stream.rs:220-266`). Tenet 4 (YAGNI) names this exact case |
| SR-12 | Per-packet `HashSet` insert on the receive hot path | P2 | S | planned | `FrameAssemblyState::received_packet_ids` is a `HashSet<u32>` (`crates/viva-genicam/src/stream.rs:456`) taking one insert per datagram (`:490`); at a 1500-byte packet size a 3.1 MB frame costs ~2 100 hashes. A grow-only bitmap or a max-ID watermark does the same job allocation-free. **Observed while reviewing [#34](https://github.com/VitalyVorobyev/viva-genicam/pull/34) on 2026-07-25 and never filed** — recorded now because an unwritten review note is indistinguishable from one that was never made. The data structure would actually be chosen in API-01 (single frame-reassembly implementation); this row exists so it is not lost if API-01 slips |

## GA — GenApi conformance, round 2 (roadmap Phase 4)

Supersedes the previous `XML` section. **Do not quote a denominator from this
section — recount it.** The counts below were measured against the corpus as it
stood when each row was written, and the corpus grows: rows say "of 35", the
header used to say 37, and the tree currently holds 38. Every one of those
numbers was right once, which is exactly why none of them should be repeated
without `ls fixtures/vendor-xml/*.xml | wc -l` for the denominator and a
whole-element match for the numerator. A `grep -c` of an opening tag
undercounts, because the FLIR and PGR documents are single-line XML — that is
how GA-09's own count came to be wrong by 7 declarations and its `<pLength>`
split wrong by a factor of eight (see the row). The counts are here to rank
work against each other, not to be published.

| ID | Task | Priority | Size | Status | Notes |
|----|------|----------|------|--------|-------|
| GA-01 | Parse and honour `pInvalidator` | P0 | L | planned | **18 502 occurrences in 32 of 35 documents** — the largest unparsed element in the corpus by an order of magnitude. Invalidation currently fires only on writes made through the NodeMap |
| GA-03 | `<pSelected>` is parsed with the direction inverted | P1 | M | planned | 1 534 occurrences in 31 docs. The standard puts it on the *selector* naming the *selected* feature (`fixtures/vendor-xml/Basler_acA1600_20gm.xml:213`); `crates/viva-genapi-xml/src/parsers/mod.rs:116-131` treats the text as this node's selector, registering invalidation edges backwards |
| GA-04 | Activate `pMin`/`pMax` | P1 | M | planned | 1 288 / 911 occurrences, 35 / 34 docs. Parsed (Integer only), stored on `IntegerNode`, registered as dependencies (`nodemap.rs:1489-1494`) — and never read. Range checks use the static limits at `nodemap.rs:271-273`, so a dynamic limit is silently ignored |
| GA-05 | Honour `Cachable` and `PollingTime` | P1 | L | planned | 2 735 / 32 docs and 327 / 28 docs, both unparsed. Every readable node is cached until a dependency is written |
| GA-07 | Honour `<Slope>` on Converter min/max propagation | P1 | S | planned | Was filed unconfirmed at P2. Confirmed: **632 occurrences in 29 of 35 documents**. A decreasing converter inverts min and max; we propagate neither |
| GA-08 | Parse `ImposedAccessMode` | P1 | S | planned | 2 709 occurrences, 28 docs, unparsed |
| GA-09 | Support the `<Register>` node type | **P1** | M | in-progress | **63 declarations across 16 of the 37 corpus documents, measured 2026-07-31.** The row previously said "56 occurrences, 14 docs" and that was an undercount: a `grep -c` of `<Register ` misses declarations in the FLIR and PGR documents, which ship as single-line XML. Count with a whole-element match, not a line count. Now visible: `XmlModel::skipped` records them, and both corpus tests carry `Register` in `EXPECTED_SKIP_TAGS` until this lands. **Raised from P2 on three independent pieces of evidence in one week.** (1) [#70](https://github.com/VitalyVorobyev/viva-genicam/issues/70)'s JAI drops exactly two nodes of 1 067 — `ChunkImage` and `FileAccessBuffer`, both `<Register>` — so this is no longer a corpus-frequency argument. (2) [#92](https://github.com/VitalyVorobyev/viva-genicam/pull/92)'s author, a different user on Micro-Epsilon hardware, proposed publishing `NodeMap::resolve_address` specifically to reach "file-transfer buffer addresses" — i.e. to work around this gap from outside the crate. (3) The Micro-Epsilon scanCONTROL 850050 description that same contributor then contributed to the corpus has four `<Register>` nodes — `FileAccessBuffer`, `ChunkMeasurementResults`, `ChunkMeasurementObjects`, `ChunkEvaluationResults` — which are its only four skips out of 772 nodes. `FileAccessBuffer` is the same node name the JAI drops, so two vendors and two device classes hit the identical node, and it is the direct explanation for (2): the node backing their file-transfer buffer is discarded before any typed accessor could reach it. **Scoped**: `<Register>` is `<StringReg>` minus UTF-8 decoding, plus one genuinely new element — `<pLength>`, a length resolved from another node at runtime, which is parsed nowhere in the workspace. `Addressing` hardcodes `len: u32` in both variants, so threading a dynamic length through it would touch every parser; cheaper to keep length off `Addressing` and put a `RegisterLength` on a new `RegisterNode`, resolved in `get_register`. That bounds the change to one parser fn, one `NodeDecl` variant, one `RegisterNode`, one `build_node` arm, two `NodeMap` methods, and dropping `"Register"` from both allowlists. **Do this in two phases, because the split is nothing like what this row used to claim.** It said "5 of the 56 corpus declarations use a plain `<Length>`"; the real figure is **42 of 63**, so a plain-`<Length>`-only first cut covers two thirds of corpus occurrences rather than 9%. It clears `FLIR_BFS_PGE_31S4C_C`, `FLIR_BFS_PGE_63S4C`, `FLIR_BFS_PGE_70S7C`, `FLIR_ORX_10G_51S5M`, `PGR_BlackflyS_13Y3M`, `MicroEpsilon_scanCONTROL_850050` and `aravis_genicam` completely, and takes both Basler documents from 10 skips to 2. The 21 `<pLength>` declarations are concentrated — Hikrobot 11, Basler 2+2, and six documents with one each — so phase 2 is a narrow follow-up, not half the work. Per ADR-0018 the deviation must be *visible*: a `<Register>` skipped for `<pLength>` has to say so in its error, and the corpus allowlist has to be scoped by that reason so no other `<Register>` failure hides behind it. `<pPort>` on a non-device port (`PortChunkImage`) stays out of scope — that is GA-12, but the accessors must **refuse** such a node rather than silently read the device port, which for the scanCONTROL's three chunk-port registers at address `0x0` would return GVCP bootstrap registers. **Phase one done 2026-07-31**: plain `<Length>` registers parse, build and are readable/writable through `NodeMap::get_register`/`set_register`; corpus goes from 35 045 to 35 087 nodes (+42), and `FLIR_BFS_PGE_31S4C_C`, `FLIR_BFS_PGE_63S4C`, `FLIR_BFS_PGE_70S7C`, `FLIR_ORX_10G_51S5M`, `PGR_BlackflyS_13Y3M`, `MicroEpsilon_scanCONTROL_850050` and `aravis_genicam` reach zero skips. Both corpus allowlists are now keyed by `(tag, error substring)` so a `<Register>` skipped for `<pLength>` passes and one skipped for any other reason still fails. **Phase two remains**: `<pLength>`, 21 declarations -- Hikrobot 11, Basler 2+2, six documents with one each. **The two-accessor split is confirmed by the user it was built for** ([#93 comment](https://github.com/VitalyVorobyev/viva-genicam/pull/93#issuecomment-5162882332)): they want both kept — `get_register`/`set_register` for ordinary named registers, and `register_address` for the bounded, chunked READMEMs a 100 000-byte `FileAccessBuffer` needs, because `get_register` reads the full declared length. Both shipped in 0.4.0 and the explicit non-device-port refusal was endorsed, so nothing in phase one needs revisiting; only `<pLength>` remains |
| GA-10 | `exec_command`'s direct-address path skips the write guards | P2 | S | planned | Loose end left by the `ensure_writable_now` predicate guards added for [#45](https://github.com/VitalyVorobyev/viva-genicam/issues/45). `NodeMap::exec_command` (`nodemap.rs:827-855`) has two paths: a `<pValue>` node delegates to `set_integer`, which `ensure_writable_now` guards, while a node with a bare `<Address>` does a raw `io.write` with no access-mode, availability or `pIsLocked` check. So the same command is refused locally or sent to the wire depending on which way its XML expresses the target — the inconsistency those guards exist to remove. **Measured against the corpus before pricing it**: all 432 `<Command>` nodes across the 35 vendor documents use `<pValue>` — *none* takes the direct-address path, and all 121 that carry a `<pIsLocked>` are therefore already guarded. No device we hold a description for can reach the gap, hence P2 rather than P0. Worth noting the inversion, though: `viva-fake-gige`'s three Command nodes are *all* direct-address, so our GigE integration tests exercise only the path no real camera uses and never the path every real camera uses — the spec-derived-fixtures shape again ([ADR-0019](adrs/adr0019-transport-conformance-and-spec-derived-fakes.md)), and an argument for switching the fake's commands to `pValue` when this is fixed |
| GA-11 | Corpus test asserts no values, so a wrong one still passes | P1 | M | in-progress | **First slice shipped with the integer codec** ([ADR-0022](adrs/adr0022-integer-register-decoding.md)): `crates/viva-genapi/tests/vendor_corpus.rs` now evaluates every document twice, once against `NullIo` and once against a test-local `PatternIo` returning a descending byte pattern (`FF FE FD …`), which sets the top bit in either byte order; in the pattern pass `GenApiError::Parse` counts as an engine defect, because every value the pattern produces is representable. That pass failed on **373 nodes across 19 of 38 documents** before the codec fix and is clean after — which is what the previous zero-only stage could not do, having stayed green through the weekly runs of 2026-08-31, 09-07 and 09-14 across a union of 448 wrong nodes. `NullIo` is unchanged; its zero contract is public API that Studio and the wasm build depend on, and the pattern stub is deliberately test-local. **What remains, and why this row is not done**: the test still asserts no *values*, only that nothing errors — a register decoded in the wrong byte order returns a plausible number and passes both passes. Per-document value expectations are the rest of the job, and they are what would catch a byte-order regression |
| GA-12 | GenApi chunk adapter | P2 | L | planned | Replaces the hardcoded 4-entry table at `crates/viva-genicam/src/chunks.rs:114-142`. `ChunkID` appears 164 times in 14 docs and is ignored; `RegisterIo` has no port abstraction to address a chunk port with |
| GA-13 | Support negative `<Address>` (chunk-relative offsets) | P2 | M | planned | Confirmed: `Baumer_HXG20` `ChunkImageLength`; the sole `EXPECTED_SKIPS` entry in both corpus tests. Needs a signed `AddressTerm::Fixed` plus a chunk base |
| GA-14 | `Streamable` — no persistence/save-load feature exists | P2 | L | planned | 1 700 occurrences, 18 docs. Feature-set save/restore is a standard GenApi capability we do not offer |
| GA-15 | Fall back to lossy decoding for non-UTF-8 GenApi XML | P2 | S | planned | Unconfirmed. `fetch.rs` `String::from_utf8` is strict, so an ISO-8859-1 document fails the connect outright. **Distinct from the UTF-8 BOM defect** fixed in 0.5.0 ([#122](https://github.com/VitalyVorobyev/viva-genicam/issues/122)), which was valid UTF-8 that parsed into nothing |
| GA-16 | Accept uppercase `0X` hex prefix in `parse_u64`/`parse_i64` | P2 | S | planned | Unconfirmed. Hex digits are already case-insensitive, only the prefix is not. No corpus document uses it |
| GA-17 | Inline `<IntSwissKnife>` as a register address term | P2 | M | planned | Unconfirmed: no corpus document nests one inside a register, but the schema allows it. Today `skip_element` drops the term silently |
| GA-18 | Decide the sign of scaled `<Float>` and `<Enumeration>` payloads | P2 | S | planned | GenICam declares no `<Sign>` for either; 0.3.0 kept the historical signed reading. Worth confirming against hardware before changing |
| GA-19 | Parse `pInc` | P2 | S | planned | 333 occurrences, 25 docs. Static `Inc` is honoured; the dynamic form is not |
| GA-23 | `<ImposedAccessMode>` is parsed nowhere | P1 | S | planned | Found while tracing the big-endian bit-numbering fix that shipped in 0.5.0. Zero occurrences in the workspace: the tag is swallowed by `skip_element`. `FLIR_BFS_PGE_31S4C_C` uses it 1000+ times — `SensorWidth` declares `<ImposedAccessMode>RO</ImposedAccessMode>` and we expose it RW. Per GenICam it is a ceiling on the node's access, not a default, so it cannot be folded into the existing `<AccessMode>` handling |
| GA-24 | `<pInvalidator>` is implemented nowhere | P1 | M | planned | Found while tracing the big-endian bit-numbering fix that shipped in 0.5.0, and the next thing [#120](https://github.com/VitalyVorobyev/viva-genicam/issues/120)'s reporter will hit. 20 826 occurrences inside register nodes across the corpus. `ExposureTime_Avl` alone declares 19, so after `set("ExposureAuto", "Off")` its cached value never refreshes. The nodemap builds invalidation edges from `pValue`, predicate refs and addressing refs only (`nodemap.rs:37-60`). Not the cause of #120 — a fresh `viva-camctl` process failed identically — but it is why the same session can go on disagreeing with the camera. `<Cachable>NoCache</Cachable>` (3 179 occurrences) is unparsed for the same reason and belongs with it |
| GA-25 | `<pMin>`/`<pMax>` are ignored for `<Float>` | P2 | S | planned | Found while tracing the big-endian bit-numbering fix that shipped in 0.5.0. `parse_integer` reads both (`parsers/numeric.rs:63,70`); `parse_float` reads only literal `<Min>`/`<Max>`, so FLIR's `ExposureTime` — which delegates both to registers — gets `f64::MIN`/`f64::MAX`. Currently harmless because the `pValue` branch returns before the range check, which makes it exactly the kind of latent gap that surfaces on the next refactor |
| GA-26 | `AccessMode::parse` maps anything unrecognised to `RW` | P2 | S | planned | Found while tracing the big-endian bit-numbering fix that shipped in 0.5.0. `lib.rs:219-226`, asserted as intended behaviour by its own test. A typo or an unknown future mode silently becomes the most permissive value; `RO` would be the safe default, and per ADR-0018 the deviation should at least be logged |
| GA-29 | SwissKnife identifiers cannot carry `.`, and admitting them is not enough | P2 | M | planned | From [#139](https://github.com/VitalyVorobyev/viva-genicam/pull/139), which needs `V1.Min` / `V1.Max` — GenICam's `<pVariable>`-with-attribute form. `lex_ident` stops at any non-alphanumeric byte (`crates/viva-genapi/src/swissknife.rs:949-963`), so the formula fails to parse and the node is skipped. Admitting `.` into the lexer changes the expression language for all 38 corpus documents and still does not work: the SwissKnife builder validates every identifier against the declared `<pVariable>` names (`crates/viva-genapi/src/nodemap.rs:1902-1911`), and the XML declares `V1`, not `V1.Min`, so the node fails one stage later with `UnknownVariable`. The task is qualified `<pVariable>` resolution, not the lexer. Ours, not the contributor's |
| GA-30 | `GenApiError::Parse` is a generic conversion bucket | P2 | S | planned | `crates/viva-genapi/src/error.rs:40-42` reads `parse error: {0}`, and `conversions.rs` raises it for runtime register-decode failures where nothing was parsed. A reporter consequently sees `GenApi(Parse(...))` for a failed device read and reasonably concludes the XML is at fault. It also means the corpus test cannot discriminate a decode failure from a real parse failure, which is half of why GA-11 matters. Adding a variant is breaking — schedule with a minor |
| GA-31 | A masked write to a write-only register reads it first | P1 | S | planned | [#135](https://github.com/VitalyVorobyev/viva-genicam/issues/135). Top-level reads *are* guarded — `ensure_readable` (`crates/viva-genapi/src/nodemap.rs:64-69`) runs before any transport call at every typed getter and `<pValue>` chains re-check at each hop — so the gap is on the **write** side. `set_integer` (`nodemap.rs:315`) and `set_bool` (`nodemap.rs:841`) implement a bitfield write as read-modify-write via `get_raw_or_read` (`crates/viva-genapi/src/conversions.rs:318-334`), which issues `io.read` at `:330` with no access check whenever the raw cache is cold. `ensure_writable_now` correctly permits `WO` — it is a write — but the write is *implemented* as a read, and the camera refuses it. The corpus-shaped case is a `<StructEntry>` under a `<StructReg AccessMode="WO">`, which inherits WO (`parsers/struct_reg.rs:142`) and always has a bitfield. Refuse only when the node is unreadable **and** the cache is cold: a warm cache is the legal GenICam path and must keep working. **This is our diagnosis of a path matching the report, not a confirmation of the reporter's case** — they attached no log |

## API — Public-surface consolidation (breaking)

| ID | Task | Priority | Size | Status | Notes |
|----|------|----------|------|--------|-------|
| API-01 | Single frame-reassembly implementation | P1 | L | planned | See SR-09. [#72](https://github.com/VitalyVorobyev/viva-genicam/pull/72) **landed** a second implementation: `WindowsFrameAssembly` (`crates/viva-genicam/src/stream.rs:545`) alongside `FrameAssemblyState` (`:437`). The review asked for reuse; merging without it was a deliberate call to avoid another round-trip on a P0 hardware fix. The duplicate is a simple sequential reassembler — no bitmap, no resend accounting, no out-of-order tolerance — so Windows silently has weaker reassembly than every other platform, which is the real cost. It also forced `#[cfg_attr(windows, allow(dead_code))]` on `FrameAssemblyState`, because the fields only its completion step reads have no reader on Windows; that allow goes away with the unification |
| API-02 | Curated public surfaces | P1 | L | planned | Kill blanket `pub mod` (viva-gige, viva-u3v, viva-service, viva-camctl, viva-fake-gige). Re-export unnameable types: `ConverterNode`, `IntConverterNode`, `StringNode` and `PredicateRefs` are public payloads of public enum variants but are not re-exported — matchable, unnameable. **Now has an outside consumer pushing on it**: [#92](https://github.com/VitalyVorobyev/viva-genicam/pull/92) proposed making `NodeMap::resolve_address` public, which would have put `viva_genapi_xml::Addressing` — and transitively `AddressTerm` and `IndexOffset` — into `viva-genapi`'s contract, none of them re-exported (`viva-genapi/src/lib.rs:20` re-exports only `AccessMode`, `SkOutput`, `SkippedNode`). It would also semver-lock the addressing model that **GA-09** (`pLength`) and **GA-13** (signed `AddressTerm::Fixed`) both plan to change, including the `(u64, u32)` return GA-09 wants to widen. Countered with a name-keyed `NodeMap::register_address(name, io)` that keeps `Addressing` private; the general lesson is that the unnameable-type debt is now visible from outside and the next request will not necessarily come with a workable alternative attached |
| API-03 | Error source chains (no `String` payloads); `#[non_exhaustive]` policy | P1 | M | planned | `thiserror` is already on 2 workspace-wide, so this is purely the API work now. Overlaps GA-30 (`GenApiError::Parse` is a generic conversion bucket) and GA-31 (`GenApiError::Access` cannot say which direction was refused, and has no test anywhere in the workspace) — both are `String`-payload variants doing two jobs |
| API-04 | Dedupe viva-service vs viva-service-u3v behind a `StreamSource` trait | P1 | L | planned | ~60% copy-paste today |
| API-05 | Typed accessors on `Camera` | P1 | M | planned | `Camera::get`/`set` are string-only (`crates/viva-genicam/src/lib.rs:195,238`) while `NodeMap` already has `get_integer`/`get_float`/`get_bool`/`get_enum`. Floats round-trip through `to_string`/`parse` |
| API-06 | Type-gate the GigE-only methods on `Camera<T>` | P1 | M | planned | `configure_events` (`lib.rs:464`) and `configure_stream_multicast` (`lib.rs:576`) write GVCP bootstrap registers but are defined on the generic `Camera<T>`, so they compile — and silently misbehave — against a U3V camera |
| API-07 | Stop exposing node cache internals | P2 | S | planned | `SkNode.cache`, `ConverterNode.cache`, `IntConverterNode.cache`, `StringNode.cache` are `pub RefCell<Option<(_, u64)>>` (`crates/viva-genapi/src/nodes.rs:329,395,424,441`), publishing the internal generation counter; `IntegerNode.cache` is `pub(crate)`. Pick one |
| API-08 | `Camera` get/set inconsistencies | P2 | S | planned | `get` on a Category returns `Ok("")` (`lib.rs:232`); `set` on a Command executes and ignores the value (`lib.rs:279-282`); Converter/IntConverter are readable but not writable through `Camera` although `NodeMap::set_converter` exists |
| API-09 | Missing `NodeMap`/`Node` accessors | P2 | S | planned | No `Node::unit()`/`min()`/`max()`/`inc()` though the data is stored; `set_string` takes `&self` while every other setter takes `&mut self`; `get_integer` handles `IntConverter` and `SwissKnife` but not `Converter`; `enum_entries` sorts and dedups, discarding the XML order GUIs need |
| API-10 | Fakes import transport-crate register constants; viva-pfnc as single `PixelFormat` authority; workspace lints (`missing_docs`, `unreachable_pub`) | P2 | M | planned | `viva-zenoh-api` defines a second `PixelFormat` bridged by `crates/viva-service/src/pixel_format.rs:7` |
| API-11 | U3V/GigE facade parity | P2 | L | planned | U3V has no events, chunks (`chunks: None` hardcoded, `stream.rs:829`), stats, time sync, or stream params; its builder takes a `Camera` where GigE's takes a device, and its `build()` is sync where GigE's is async |
| API-13 | `viva-zenoh-api`'s announce types have no `#[non_exhaustive]` | P1 | S | planned | `DeviceAnnounce` and its siblings are all-public-field structs (`crates/viva-zenoh-api/src/lib.rs:31-42`), so every field addition is a breaking change and a minor bump — which is exactly what SVC-07 needs. 0.4.0 applied `#[non_exhaustive]` to the five enums that grow (`Node`, `NodeDecl`, `ChunkKind`, `ChunkValue`, `ChunkError`) and never reached this crate. Do it inside SVC-07, while the struct is already breaking; it cannot be done in a patch |

## SVC — Services (roadmap Phase 6)

| ID | Task | Priority | Size | Status | Notes |
|----|------|----------|------|--------|-------|
| SVC-01 | Announce cadence exceeds Studio's expiry window | P1 | S | planned | GigE re-announces every `discovery_timeout` + `discovery_interval` ≈ 7 s (`crates/viva-service/src/config.rs:15-20`); Studio expires at 6 s (`studio/apps/viva-studio-tauri/src-tauri/src/commands/device.rs:339`) and `docs/studio/zenoh-api.md:17-32` documents 2 s. Devices can flicker |
| SVC-02 | U3V introspection is typeless | P1 | M | planned | `U3vDeviceHandle` never overrides `DeviceOps::get_feature_state`, so the degraded default (`crates/viva-service/src/device.rs:42-54`: `access_mode: "RW"`, `kind: "Unknown"`, no ranges) serves every U3V camera |
| SVC-03 | U3V service streaming never configures the SIRM | P1 | M | planned | `crates/viva-service-u3v/src/device.rs:39-48` builds `U3vStream::new` directly with hardcoded 256-byte leader/trailer, bypassing `U3vDevice::open_stream` (`crates/viva-u3v/src/device.rs:159-183`) which reads the SIRM and enables streaming |
| SVC-04 | U3V service is single-device with no lifecycle | P2 | M | planned | Takes `devices[0]` once, never re-scans, never publishes disconnect (`crates/viva-service-u3v/src/main.rs:247-253`); GigE has a full discovery loop |
| SVC-05 | GigE service clap name is still `genicam-service` | P2 | S | planned | `crates/viva-service/src/config.rs:6`, pre-rebrand |
| SVC-07 | `publish_announce` reports the device id as the serial and drops everything else | P1 | S | planned | [#137](https://github.com/VitalyVorobyev/viva-genicam/issues/137). `crates/viva-service/src/main.rs:255-268` takes `(device_id, model)` and sets `serial: device_id.to_string()` — i.e. `cam-<mac hex>`, not the camera's serial — although the full `DeviceInfo` is in scope at both call sites (`:122`, `:142`) carrying `ip`, `mac`, `serial` and `user_name` (`crates/viva-gige/src/gvcp.rs:305-315`). `viva-service-u3v` passes a real serial (`crates/viva-service-u3v/src/main.rs:170-175`); the GigE service does not. So two identical cameras announce `name == model == <same string>` and differ only in a field that is both wrong and unrendered. Needs `DeviceAnnounce` extended and `API_VERSION` bumped from 2 (`crates/viva-zenoh-api/src/lib.rs:27-42`) — do API-13 in the same change. Discovery has parsed the serial and the user-defined name since 0.3.0, on the argument that users identify a camera by the label on its case; they were never plumbed past the library |
| SVC-08 | One write-only node fails an entire feature snapshot | P1 | S | planned | [#135](https://github.com/VitalyVorobyev/viva-genicam/issues/135). `build_feature_state` computes the access mode (`crates/viva-service/src/device.rs:326-332`) and then reads the value unconditionally with `?` (`:334-353`), so a single `WO` node makes the whole snapshot `Err`. The near-duplicate in Studio's embedded backend does the same (`studio/apps/viva-studio-tauri/src-tauri/src/backend/embedded.rs:679-800`, `:807-814`); `bulk_feature_state` (`embedded.rs:252-274`) instead downgrades to `warn!` and omits the feature, so there the symptom is a silently missing feature. Same shape as ST-21, and the two should land together |

## DC — Device classes beyond area-scan

A laser profile scanner joined the corpus on 2026-07-31 (Micro-Epsilon
scanCONTROL 850050, contributed alongside
[#93](https://github.com/VitalyVorobyev/viva-genicam/pull/93)), and #93 added
the six PFNC codes it needs. **Adding the format codes is not the same as
supporting the device class.** Nothing above `viva-pfnc` knows what a
`Coord3D_*` frame is; these rows are what that costs, each measured against
that document or against the code rather than against the idea of a 3D camera.

Only the frame-sizing half has been fixed (0.4.0). The rest are filed rather than built because
nobody has reported *streaming* one of these devices through this stack, and
the evidence hierarchy puts a hardware report above our inference — DC-04 in
particular would have us guess what the wire looks like, which is the ADR-0019
failure of fake and client agreeing only with each other.

| ID | Task | Priority | Size | Status | Notes |
|----|------|----------|------|--------|-------|
| DC-02 | The Zenoh wire format carries no `Coord3D_*` variants | P2 | M | planned | `viva_zenoh_api::PixelFormat` (`crates/viva-zenoh-api/src/lib.rs:253-283`) has exactly one 3D entry, `Coord3D_C16` — which the scanCONTROL does not advertise — and none of the four it does. `#[serde(other)] Unknown` means `ImageMeta.pixel_format` decodes any of them to `Unknown` without complaint, so `payload_size` is announced as `w * h * 1` (`crates/viva-service/src/acquisition.rs:419-428`). Extending it changes the one-byte discriminant map (`crates/viva-zenoh-api/src/frame_header.rs:108-138`, populated to 26), so it is a wire-format change rather than a local fix, and it should land with DC-03 so the published values mean something |
| DC-03 | No `Scan3d*` SFNC constants, so a `Coord3D` payload cannot be interpreted | P2 | M | planned | `crates/viva-sfnc/src/lib.rs` defines exactly one constant, `CHUNK_MODE_ACTIVE`; `Scan3d` appears in no source file in either workspace. `MicroEpsilon_scanCONTROL_850050.xml:6104+` carries the full block — `Scan3dCoordinateScale`, `Scan3dCoordinateOffset`, `Scan3dInvalidDataFlag`, `Scan3dInvalidDataValue`, `Scan3dOutputMode`, `Scan3dCoordinateSelector` — and the document itself states at `:6188` and `:6218` that scale and offset apply only when `PixelFormat` is `Coord3D_AC16`. Without reading them, a perfectly received `Coord3D_AC16` buffer is raw integers with no metric meaning and no way to mask invalid points. The nodes already parse as generic `Float`/`Enumeration`; what is missing is anything that reads them. Also unread: `DeviceScanType` (`:40-61`), which offers `Areascan3D` and `Linescan3D`. **`Scan3dInvalidDataValue` now has a confirmed on-wire value** ([#93 comment](https://github.com/VitalyVorobyev/viva-genicam/pull/93#issuecomment-5162882332)): little-endian `f32` `ff ff ff 7e` = `1.7014117e38`, observed in a raw capture and matching what the node declares — so masking invalid points can be tested against a real value rather than an assumed sentinel |
| DC-04 | No fake can emit a `Coord3D_*` frame | P2 | M | planned | `viva-fake-gige` sends only `PAYLOAD_IMAGE` (`crates/viva-fake-gige/src/gvsp_sender.rs:270`, `:339`) and `viva-fake-u3v` hardcodes bytes-per-pixel as `{RGB8 => 3, _ => 1}` (`crates/viva-fake-u3v/src/transport.rs:152-156`), so DC-02 and DC-03 have no end-to-end test and neither will any fix for them. The same shape as the fake's `ChunkModeActive` type error, fixed in 0.4.0: the fake cannot reach the code path, so the path stays unproven. Deliberately P2 — do this once a reporter tells us what a scanCONTROL actually puts in its GVSP leader, because a fake built on our guess would confirm the guess. **That condition was met on 2026-08-03** ([#93 comment](https://github.com/VitalyVorobyev/viva-genicam/pull/93#issuecomment-5162882332)). The contributor captured the wire: leader payload type `0x4001` (image with the extended-chunk bit — neither GenDC nor multipart), PFNC `0x026000C0`, coordinates little-endian and interleaved per point at 4 / 4 / 8 / 12 bytes for `Coord3D_C32f` / `_AC16` / `_AC32f` / `_ABC32f` — which independently confirms `viva-pfnc`'s `bytes_per_pixel` (`crates/viva-pfnc/src/lib.rs:102-106`), previously cross-checked only against the Harvesters symbolics table — plus four decoded `(A, B, C)` tuples in which `B` is zero for a single profile. The fake can now be built against a capture instead of a guess, so this is unblocked; it stays P2 only because GA-09 phase two is ahead of it |
| DC-05 | An event-vision block is published on the image topic as a 1×64000 image | P1 | M | planned | From [#138](https://github.com/VitalyVorobyev/viva-genicam/issues/138) / [#139](https://github.com/VitalyVorobyev/viva-genicam/pull/139) (Lucid Triton2 EVS, Sony IMX636). `crates/viva-service/src/acquisition.rs` calls `next_frame()`, so an event block arrives as a `Frame` with `width = 1`, `height = 64000` and an unnamed PFNC code; the accompanying `expected_payload_len → None` stops it being dropped as undersized, so it is published rather than discarded — with a `FrameHeader` describing something that is not an image and `PixelFormat::Unknown`. Which topic a non-image stream belongs on is a `viva-service` design question and is ours, not the contributor's. Same class as the rest of this section: the device class does not exist above the payload layer. Also ours from that PR: facade re-exports for the block types, a Python `next_block`, and a test for the `#[cfg(windows)]` receiver branch |

## CI

| ID | Task | Priority | Size | Status | Notes |
|----|------|----------|------|--------|-------|
| CI-01 | Per-crate feature matrix (`cargo hack --each-feature`) | P1 | M | planned | **Restated** — the previous wording ("U3V is never compiled in CI") was inverted. `viva-camctl`/`viva-pygenicam`/`viva-service-u3v` all enable `u3v-usb`, so unification builds it. The real gap: `viva-genicam` with its *own* default features (`[]`) is never verified |
| CI-03 | MSRV job | P2 | S | planned | `rust-version = "1.88"` is declared and never checked |
| CI-04 | Windows wheel is built and published but never tested | P2 | M | planned | `python.yml` test matrix is ubuntu + macos-14 only. #57 is a Windows issue |
| CI-05 | Scope release.yml / publish-docs.yml permissions to the jobs that need write | P2 | S | planned | |
| CI-06 | Drop hardcoded `sleep 30` in publish-crates.yml | P2 | S | planned | cargo ≥ 1.66 waits for the index |
| CI-07 | python.yml path filter misses viva-gencp and root Cargo.toml/Cargo.lock | P2 | S | planned | |
| CI-08 | `cargo doc` in ci.yml lacks `--all-features` + `RUSTDOCFLAGS` | P2 | S | planned | |
| CI-09 | Fuzz the packet and XML parsers | P2 | L | planned | Both consume untrusted network input |
| CI-10 | cargo-semver-checks on release tags | P2 | M | planned | |
| CI-17 | Nothing checks links in `docs/` | P2 | S | planned | Seven files linked to `docs/zenoh-api.md` and `docs/camera-service-api.md` for weeks after both moved under `docs/studio/`, and nothing noticed. `crates/viva-genicam/tests/book_includes.rs` covers mdBook `{{#include}}` anchors only, and `ci.yml` greps `mdbook build` for `[ERROR]` — neither looks at ordinary Markdown links, and neither looks outside `book/` |

## DOC — Documentation

Several of these are *wrong* documentation rather than missing documentation,
which is why they are not all P2.

| ID | Task | Priority | Size | Status | Notes |
|----|------|----------|------|--------|-------|
| DOC-05 | Book: empty chapters and broken links | P2 | M | in-progress | **Links done**: `api.md` was rewritten — two of its five rustdoc links were 404s on the live site (`api/viva-genicam/` with hyphens where rustdoc emits underscores, and `api/genicp/`, a crate name that never existed) and seven published crates were unlisted; all thirteen are now listed and were checked against the deployed site with `curl`. `welcome.md:17`'s link to the retired external genicam-studio repo is gone, as is the same link in `crates/viva-service/README.md:5`. `crates/README.md`'s quick map listed 6 of 15 crates with pre-rebrand paths (`crates/genapi-xml`, `crates/genicam`) that do not exist; it now lists all 15. **Still open**: `errors-logging.md` and `contributing.md` are 24-line stubs — that is DOC-06's kind of work (writing new documentation), not this pass's |
| DOC-06 | Book: fill the three crate pages; add the missing ones | P2 | L | planned | No page exists for viva-u3v, viva-genapi-xml, viva-pfnc, viva-sfnc, viva-zenoh-api, the services, viva-camctl or the fakes. USB3 Vision has no tutorial anywhere |
| DOC-11 | Python `frame.ts_host` is silently wrong for GigE | P1 | S | planned | `build_gige_stream` always passes `Some(time_sync)` (`crates/viva-pygenicam/src/stream.rs:96-99`), but `time_calibrate` is not exposed and `TimeSync::to_host_time` with no origin returns `SystemTime::now()` (`crates/viva-gige/src/time.rs:308-312`). Always wall-clock, never device-mapped — worse than `None`, because it looks like data. Fix the behaviour or the docs, not neither |

## ST — Studio

| ID | Task | Priority | Size | Status | Notes |
|----|------|----------|------|--------|-------|
| ST-01 | Modernize studio crates to edition 2024 + workspace-dep inheritance | P2 | M | planned | |
| ST-02 | Retire apps/viva-mock-service in favor of viva-service + viva-fake-gige | P2 | M | planned | |
| ST-03 | Revive studio e2e against in-repo service/fake (drop aravis-from-source) | P1 | L | planned | |
| ST-06 | Release packaging pipeline: DMG (macOS), AppImage (Linux), MSI (Windows) on tag push | P1 | L | planned | M11 |
| ST-07 | Bundle viva-service binary as Tauri sidecar (auto-start if no external service) | P1 | M | planned | M11 |
| ST-08 | Frame annotation rendering engine (frame ID/timestamp/FPS burned into BMP stream) | P2 | M | planned | M11 |
| ST-09 | Annotation toggle in viewer toolbar | P2 | S | planned | M11 |
| ST-10 | Recording playback engine: load .gsr, play/pause, frame step, speed, seek | P2 | L | planned | M12 |
| ST-11 | Recording export to TIFF stack / uncompressed AVI | P2 | M | planned | M12; interop with ImageJ, MATLAB |
| ST-12 | Auto-update via Tauri v2 updater plugin | P2 | M | planned | M12 |
| ST-13 | Studio performance benchmarks in CI | P2 | M | planned | M12; fail on >10% regression |
| ST-14 | Embedded backend has no U3V discovery | P2 | M | planned | `backend/embedded.rs:122-123` reads only the GigE cache despite `u3v-usb` being enabled |
| ST-17 | Cover the `StreamInfo` re-send on a mid-stream format change | P2 | S | planned | Promised to the contributor on [#87](https://github.com/VitalyVorobyev/viva-genicam/pull/87), ours to do. That PR fixed Studio reporting a hardcoded `Mono8` — it now seeds `StreamInfo` from `PixelFormat` before acquisition and re-sends when a frame disagrees — but the test it shipped with (`stream_info_preserves_camera_pixel_format`) exercises the six-line constructor, not the behaviour. Pinning the re-send needs a frame fixture. While there, fold the duplicated width/height comparison in `start_acquisition`: the same condition is evaluated once to rebuild the BMP encoders and again to decide whether to re-send |
| ST-18 | Studio shows enum entries the camera currently rejects | P2 | S | planned | `build_feature_state` (`backend/embedded.rs:773-780`) calls `Camera::enum_entries`, which returns every entry the XML declares. `NodeMap::available_enum_entries` (`nodemap.rs:660`) applies the `pIsAvailable` gating and exists precisely for this — the comment at that call site described it as future work, which it no longer is. A user is currently offered `PixelFormat` values the camera will refuse |
| ST-21 | Bulk feature read touches command-backed registers | P2 | S | planned | **Seen on hardware** ([#112](https://github.com/VitalyVorobyev/viva-genicam/issues/112), attached Studio log): `bulk_feature_state` reads `pAcquisitionStartReg` and logs `access denied for node`, **18** times across the run. A command's backing register is not a value to display — reading it is at best meaningless and at worst side-effecting, since some devices treat a read of a command register as significant. The node kind is known (`Node::kind_name()`), so the fix is to skip commands and their `p*Reg` targets rather than to swallow the error. The wider point is the log itself: **297 WARN lines in one session**, of which 142 are these three classes (the integer codec, TC-21, this row). A log that noisy is a log nobody reads, which is how the actual streaming failure in #112 went undiagnosed long enough to need a custom build |
| ST-22 | Studio cannot set the GVSP packet size | **P1** | S | planned | **Seen on hardware** ([#112](https://github.com/VitalyVorobyev/viva-genicam/issues/112)): on a link whose real ceiling is 9198, Studio picks the host MTU on every Acquisition Start and streams nothing, and the user has no way to say otherwise — `embedded.rs` calls `StreamBuilder::build()` with no `.packet_size(...)` and the UI exposes no control. `viva-camctl stream --packet-size 9198` works today, so the capability exists everywhere except where that reporter actually runs the software. The GVSP test-packet probe (0.5.0) is the real fix and this is not merely a stopgap: a user who knows their link should be able to state it, and a probe should be overridable by someone who has measured the answer. Small — the builder argument already exists; this is a field in the connect/acquire payload and a control beside it |
| ST-24 | The device list renders nothing that distinguishes two identical cameras | P1 | S | planned | [#137](https://github.com/VitalyVorobyev/viva-genicam/issues/137). `DeviceDropdown.tsx:132-139` is the only live device list (rendered from `AppLayout.tsx:281`) and shows a status dot — always `--idle`, so identical for every entry — plus `name`, `model` and a transport badge. It renders neither `serial` nor `id`, **both of which are already in the type** (`studio/ui/viva-studio-ui/src/device/types.ts:4-12`). `DeviceSidebar.tsx` and `DeviceCard.tsx` in the same folder are unreferenced and `DeviceCard` is the renderer that *does* show serial and id, which is the trap: the next reader will fix the dead file. Delete them or promote them, not both. Show the user-defined name when present, else the serial, else the address. Needs SVC-07 to have anything to show in remote mode, and `viva-fake-gige` needs `--serial`/`--user-name`/`--model` — `FAKE_MODEL`/`FAKE_SERIAL`/`FAKE_USER_NAME` are constants (`crates/viva-fake-gige/src/gvcp_server.rs:53-59`), so two fakes are byte-identical in identity and can reproduce the bug but not demonstrate the fix |
| ST-25 | The two backends announce different identities for the same camera | P2 | S | planned | Embedded mode sets `id` to the camera's IP and falls back to the MAC for a missing serial (`studio/apps/viva-studio-tauri/src-tauri/src/backend/embedded.rs:628-658`); the Zenoh bridge sets `id` to `cam-<mac hex>` and `name` to the bare model. So the same device changes identity when a user switches modes, and neither mode carries the IP as a field. Fold into SVC-07/ST-24 once `DeviceAnnounce` carries `ip` |
